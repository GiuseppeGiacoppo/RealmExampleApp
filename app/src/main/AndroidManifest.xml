<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.giacoppo.realmexampleapp">

    <application
        android:name=".RealmExampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.portafogli.WalletActivity" />
        <activity android:name=".ui.portafogli.SingleAccountActivity" />
        <activity android:name=".ui.comparison.ComparisonActivity"></activity>
    </application>

</manifest>